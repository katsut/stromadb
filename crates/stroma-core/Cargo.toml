[package]
name = "stroma-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "StromaDB core engine: Fact model, ontology catalog, fold, and query."

[dependencies]

[dev-dependencies]
proptest = "1"
libc = "0.2" # examples only: fcntl(F_NOCACHE) to measure uncached SSD reads (ann_ssd_p99)
