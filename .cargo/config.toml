# Linux/CI: use the mold linker (2-5x faster link than the default bfd/gold).
# macOS uses the default linker (fast enough) — intentionally not configured here.
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
